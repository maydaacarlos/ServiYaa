server:
  port: 8084

spring:
  datasource:
    url: jdbc:mysql://${SERVICEREQUEST_DB_HOST:mysql-service-request}:${SERVICEREQUEST_DB_PORT:3306}/${SERVICEREQUEST_DB_NAME:db_service_request}?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: ${SERVICEREQUEST_DB_USERNAME}
    password: ${SERVICEREQUEST_DB_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
  flyway:
    enabled: true

security:
  jwt:
    secret: ${JWT_SECRET}

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_URL:http://registry-server-prod:7082/eureka}

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus
  endpoint:
    health:
      show-details: never
  prometheus:
    metrics:
      export:
        enabled: true
  metrics:
    tags:
      application: ms-service-request

logging:
  level:
    root: INFO
  file:
    name: ./logs/ms-service-request.log

springdoc:
  api-docs:
    enabled: true
  swagger-ui:
    enabled: true

serviya:
  internal-token: ${INTERNAL_SERVICE_TOKEN}
